{
  "name": "nextstep-functions",
  "description": "Cloud Functions for NextStep notifications (in-app + email + Kit)",
  "engines": {
    "node": "24.x"
  },
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0",
    "@sendgrid/mail": "^8.1.0",
    "node-fetch": "^2.7.0"
  },
  "private": true
}
